{% if items %}
                <div class="icons_text">
                    {% for item in items %}
                    <div class="icon_text">
                        <div class="icon_text_img">
                            <img src="/image/{{ item.image }}">
                        </div>
                        {{ item.html }}
                    </div>
                    {% endfor %}
                </div>
{% endif %}